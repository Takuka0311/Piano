# 后续全量执行计划（M4+）

本文档汇总当前版本之后的所有主要工作，作为统一执行清单。

## 0. 当前完成基线
- M1：键位映射 + 曲谱调度 + CLI 闭环（已完成）。
- M2：WASAPI 最小发声、基础测试、CI 打包（已完成）。
- M3：持续混音、运行时健康检测与回退、CI 快慢分层（已完成）。
- M4：GUI Alpha（Win32）已完成。
- M5：稳定性与兼容增强（DirectSound 回退、恢复策略、配置补齐）已完成。
- M6：MIDI Out + VST2.4 最小宿主链路（CLI+GUI）已完成，进入持续增强阶段。
- M7：导出与可观测性（WAV 导出、运行诊断、导出冒烟）已完成。

---

## M6+：乐器与 MIDI 能力持续增强

### 目标
- 完整补齐 MIDI Out / VSTi 核心能力，增强演奏与兼容性。

### 关键任务
1. 增强 MIDI Out 设备枚举与 UI 下拉交互能力。
2. 增强 VST 宿主能力（参数控制、稳定性、兼容覆盖）。
3. 强化输出路径切换与异常恢复回归。
4. 扩展插件与设备矩阵的脚本化验证。

### 验收
- 在更多设备/插件组合下保持稳定可用。
- 将 `spec-004` 从 `review` 推进到 `implemented`。

---

## M7：导出与可观测性（已完成）

### 目标
- 增强工程可维护性与可交付能力。

### 关键任务
1. WAV 导出最小实现（离线渲染链）。
2. 性能/延迟指标采样与日志结构化。
3. 关键运行指标展示（缓冲占用、xruns/异常计数）。
4. Release 前自动化检查脚本增强（本地 + CI）。

### 验收
- 可导出可复现的 WAV 文件。
- 关键性能与异常可追踪。

---

## 持续任务（贯穿所有里程碑）

1. **Spec 同步**
   - 任何行为变化先改 spec，再改代码。
2. **测试护栏**
   - 每个里程碑都增加对应 CTest 与回归样例。
3. **CI 策略**
   - 默认 PR 流水线保持快速；
   - 长时发声、设备相关用例放到本地或手动触发。
4. **文档一致性**
   - 每次阶段完成后同步：
     - `README.md`
     - `docs/rebuild/*-implementation-notes.md`
     - 相关 `docs/specs/spec-xxx-*.md`

---

## 建议执行顺序
- 优先：M6（MIDI/VSTi）
- 后续：M8（导出质量与可观测深化）

> 原则：先可用、再稳、后扩展。
